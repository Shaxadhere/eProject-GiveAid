@model IEnumerable<GiveAid.Models.tbl_NGO>

@{
    ViewBag.Title = "All NGOs";
}

<script type="text/javascript">
    $(document).ready(function () {
        $('#listofall').DataTable();
    });
</script>


<div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
    <div class="card">
        <div class="card-body">
            <table class="table table-hover" id="listofall">
                <thead>
                    <tr>
                        <th scope="col">
                            @Ajax.ActionLink("Add New", "Create", "NGO",
                            new AjaxOptions()
                            {
                                HttpMethod = "GET",
                                InsertionMode = InsertionMode.Replace,
                                UpdateTargetId = "modal-container"
                            },
                            new
                            {
                                @class = "btn btn-outline-success btn-sm"
                            })
                        </th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col">@Html.ActionLink("Trash Box", "TrashBox", "NGO", new { @class = "btn btn-outline-dark btn-sm" })</th>
                    </tr>
                    <tr>
                        <th scope="col">@Html.DisplayNameFor(model => model.NGOName)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.Website)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.Email)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.Phone)</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {

                        <tr>
                            <td>@item.NGOName</td>
                            <td>@item.Website</td>
                            <td>@item.Email</td>
                            <td>@item.Phone</td>
                            <td>
                                @Ajax.ActionLink("View Details", "Details", "NGO",
                                   new { id = item.PK_ID },
                                   new AjaxOptions()
                                   {
                                       HttpMethod = "GET",
                                       InsertionMode = InsertionMode.Replace,
                                       UpdateTargetId = "modal-container"
                                   },
                                   new
                                   {
                                       @class = "btn btn-outline-primary btn-sm"
                                   })

                            </td>
                        </tr>
                                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
